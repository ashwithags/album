import { AppIfNotDirective } from './app-if-not.directive';

describe('AppIfNotDirective', () => {
  it('should create an instance', () => {
    const directive = new AppIfNotDirective();
    expect(directive).toBeTruthy();
  });
});
